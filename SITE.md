# Mindful Guided Walks

> Two hours. Half a mile. A tea ceremony among the trees.

## Brand Identity

- **Personality:** Grounded, unhurried, sincere — a wellness practice, not a hike or a spa.
- **Audience:** Women 45+, mostly on mobile. Body text is never below 16px and every text/background pairing is checked against WCAG AA.
- **Colors:**
  - Birch `#F1F0E4` — page background
  - Lichen `#CBD4B4` — fills and cards
  - Fern `#6E8C4F` — primary accent, button fills only (fails AA as text)
  - Deep Moss `#33482F` — dark bands and hover states
  - Loam `#5B4633` — earth tone, borders
  - Forest Ink `#141C13` — headings and body text
- **Fonts:** Fraunces at weight 500 (headings + both wordmarks), Literata Regular (body) — both loaded from Google Fonts via `next/font` as full variable fonts. (We tried pinning Fraunces' WONK axis to 0 for more conventional letterforms, then removed it — Squarespace's font picker doesn't expose variable-font axis controls, so it wouldn't have survived the move and wasn't worth depending on.)
- **Buttons:** Leaf-shaped radius (22px/2px/22px/2px, sharp corners top-right and bottom-left). One fill rule, no exceptions: Fern on light backgrounds, Lichen on dark backgrounds, Forest Ink text throughout. Hover on dark backgrounds brightens to Birch (on light backgrounds, hover darkens to Deep Moss). (Superseded an earlier rule that reserved Birch fill for the hero's primary action — that button is now Lichen like every other dark-background button, so there are no longer exceptions to the single rule.)
- **Text links** (as opposed to buttons): underlined, Loam on light backgrounds / Lichen on dark, with a Deep Moss/Birch hover shift. Used for secondary, non-CTA actions like "What to expect" and "See all dates."
- **Text measures:** two shared widths only — 672px for section intros, 448px for supporting/list body copy.
- **Heading hierarchy:** two sizes — primary (48px, used for "What a walk is" and "Upcoming dates" only) and supporting (36px, everything else).
- **Eyebrows:** 16px, weight 500, 1.6px letter-spacing, uppercase — identical everywhere except color: Loam on light backgrounds (Birch/Lichen sections), Lichen on dark backgrounds (Deep Moss sections, and the Loam card in Corporate). Never the same color as the heading beside it.

## Standing Rules

- **Breakpoint scoping.** Any change specified for a single breakpoint must be scoped to that breakpoint only. A mobile change must not alter rendering above 768px. A desktop change must not alter rendering below it. Before reporting any breakpoint-specific change as complete, verify the untouched breakpoints still render as they did before, and state that verification explicitly in the report. (This rule exists because a mobile-only heading change altered the desktop hero and broke it on a live site during client review — the heading ran across the photograph instead of staying inside the color panel.)

## Pages

- **Homepage** (`/`) — fully built. See section breakdown below.
- **Walks** (`/walks`), **Dates** (`/dates`), **Bedrock Babes** (`/bedrock-babes`), **Corporate** (`/corporate`), **About** (`/about`), **Contact** (`/contact`), **Liability waiver** (`/liability-waiver`) — linked from navigation and footer, **not yet built**. Visiting them in the preview will 404 until we build them.

## Homepage Sections

1. **Hero** — full-bleed split layout matching the approved Figma export: 45% Deep Moss color panel (Birch heading/subline, Lichen eyebrow, vertically centered) / 55% full-bleed forest photo. Both buttons follow the sitewide fill rule: "Upcoming dates" is Lichen (dark-background fill), "What to expect" is the outlined secondary. Desktop height targets 78vh (clamped 640–780px); eyebrow 16px/1.6px tracking; heading 48px; buttons full desktop padding. Mobile (below 768px) has its own tuning so the heading and both buttons are always visible without scrolling, with a deliberate spacing rhythm instead of a uniform 8–16px mass: hero capped at 88vh total, photo ~43% of that (~349–363px tall, full width, no inset), eyebrow 13px/1.4px tracking, heading 26px (wraps to exactly 3 lines at both 375px and 390px — confirmed by measurement, not assumed; 30px had still wrapped to 4), and a fixed gap scale — image to eyebrow 32px (a deliberately larger gap so the text reads as a new block, not a caption stuck to the photo), eyebrow to heading 12px, heading to subline 16px, subline to buttons 28px, buttons to section bottom 40px. Buttons hug their labels side by side (12px gap) instead of stacking full-width. Photo is the light-rays-through-the-trees image (chosen over a warm-trunks alternative for its visible path and because its own contrast sits with the Deep Moss panel as one scene); focal point is `50% 50%` on mobile (keeps the light rays in frame — 50% 75% cropped them out at the shorter mobile height) and `50% 75%` on desktop (keeps the path and forest floor in frame there instead). `saturate(0.85)` throughout. Buttons jump to on-page anchors (`#dates`, `#what-a-walk-is`).
2. **What a walk is** — Birch background. Three columns side by side on desktop (two-then-wrap on tablet, stacked on mobile), each with an image placeholder (4:5), an oversized "01/02/03" numeral in Loam at 40% opacity (quiet, decorative — reads as a background device, not competing with the headings; changed from Lichen, see Recent Changes), then the step heading and body (We gather / We walk slowly / We sit for tea).
3. **Upcoming dates** (`#dates`) — Lichen background, three walk cards (image, date, location, time, price) — card details are placeholder data, see below.
4. **Meet the guide** (`#guide`) — Birch background, portrait + placeholder bio for Terry.
5. **Why it works** (`#why-it-works`) — Deep Moss background. Eyebrow/heading/shortened intro full width, then three short findings (Lower blood pressure / Less stress hormone / Better sleep) spread across the full container in three columns (two-then-wrap on tablet, stacked on mobile) instead of everything crammed into the left half. Finding headings are Birch, bodies are Lichen (quieter, sits back from the heading) — both comfortably pass AA on Deep Moss (8.69:1 and 6.46:1).
6. **Bedrock Babes** (`#bedrock-babes`) — Birch background, centered, extra whitespace, one photo slot.
7. **Corporate** (`#corporate`) — Birch section with a Loam card (rounded, hugs its content) holding the copy and Enquire button, so it doesn't read as an empty full-bleed color band.
8. **Email signup** (`#signup`) — Lichen background. A static facts row ("TWO HOURS · HALF A MILE · SMALL GROUPS · TEA AT THE END") sits above a Birch card containing the heading, a 560px-wide email field + Sign me up button (front-end only, not wired to an email service yet), and a reassurance line.
9. **Footer** — wordmark, tagline, location, repeated nav, Liability waiver + Instagram links.

**Section colour sequence (top to bottom):** Hero (Deep Moss) → What a walk is (Birch) → Upcoming dates (Lichen) → Meet the guide (Birch) → Why it works (Deep Moss) → Bedrock Babes (Birch) → Corporate (Birch, Loam card inset) → Email signup (Lichen) → Footer (Deep Moss). No two adjacent sections share a saturated background color — Birch (the neutral base) is the only color that ever repeats back-to-back (Bedrock Babes → Corporate).

## Components

- **Navbar** (`components/Navbar.tsx`) — wordmark, nav links, Contact button, mobile hamburger menu. Lives in the root layout so it's on every page. The wordmark link carries `py-2` so its 28px line-height reaches the accessible 44px tap-target minimum on mobile.
- **Footer** (`components/Footer.tsx`) — also in the root layout. Every link (nav row + Liability waiver + Instagram) carries `py-2.5` so each reaches the accessible 44px tap-target minimum on mobile without changing the visible text size.
- **Button** (`components/Button.tsx`) — primary / primary-inverted / secondary / secondary-inverted variants. Single fill rule, no exceptions: `primary` = Fern fill for light backgrounds (hover darkens to Deep Moss); `primary-inverted` = Lichen fill for dark backgrounds (hover brightens to Birch). Forest Ink text throughout both. `secondary` / `secondary-inverted` are the matching outlined (no-fill) treatments. Reserved for genuine actions; anything read-more should be a TextLink, not a Button (see the audit note in Recent Changes).
- **TextLink** (`components/TextLink.tsx`) — underlined inline links, separate from buttons. Used for every "read more" / secondary link on the page: "What to expect" is the one exception that's a button, by explicit design (matches the Figma hero and pairs visually with the primary CTA beside it). Carries `py-2.5` (10px top/bottom) so its tap target reaches the accessible 44px minimum on mobile without changing the visible text size.
- **PhotoSlot** (`components/PhotoSlot.tsx`) — placeholder for photography, until real photos are supplied. Box fill is always the opposite of the color actually touching its edges (`background` prop: `"birch"` → Lichen box, `"lichen"` → Birch box, `"dark"` → Lichen box), so it never disappears against its surroundings — this is a `background` prop describing what's around the slot, not a class name. 1px Loam/40 border, rounded-lg corners (matches the site's cards), a small line-art image icon (inline SVG, Loam/50 stroke), and a 14px Loam label below it, "Photograph" by default (override via the `label` prop, e.g. "Portrait to come" for the guide).
- **EmailSignupForm** (`components/EmailSignupForm.tsx`) — the signup form. Input and button match exactly: same height (48px), same rounded-lg corners (8px) on both — the button uses plain rounded-lg here instead of the sitewide leaf-shape, since it's a form control paired directly with the input, not a standalone CTA.

## Content still needed from you

**Placeholder text on the page (clearly marked, needs your real copy):**
- Guide bio in "Meet the guide" — needs 2-3 real sentences about Terry.
- All three walk cards in "Upcoming dates" — need real date, location, time, and price for each.
- Instagram footer link currently points to `#` — needs your real Instagram URL.

**Image slots still needed (all currently gray "Photograph" placeholders):**

| Slot | Where | Aspect ratio | Minimum file size (2x) |
|---|---|---|---|
| Guide portrait | Meet the guide section | 4:5 (portrait) | 960×1200px |
| Walk card photos (×3) | Upcoming dates cards | 1:1 (square) | 800×800px |
| Bedrock Babes photo | Bedrock Babes section | 3:2 (landscape) | 1600×1067px |
| "What a walk is" step photos (×3) | We gather / We walk slowly / We sit for tea columns | 4:5 (portrait) | 800×1000px |

Once you send over photos and the missing text, I'll drop them in.

**Hero photo — done, real photography in place.** `public/images/hero-forest.jpg` is your supplied Unsplash forest photo — the light-rays-through-the-trees one with the path leading into frame (2000×3000, resized down from the original 4000×6000 for repo size — still far above what's needed at any screen size). Chosen over the warm-trunks alternative because it shows a clear path and its own internal contrast reads as one scene with the Deep Moss panel rather than competing with it. The hero is a full-bleed split: on mobile the photo runs full width above the text at roughly 33% of the hero's height; from tablet width up, it fills the right 55% of the section — edge to edge horizontally, and from the bottom of the nav to the bottom of the section vertically. If you ever need to supply a replacement: **portrait orientation preferred (this photo's own 2:3 ratio crops most gently), minimum 2000px on the longer side.**

**The other real photo is kept, unused, at `public/images/forest-trunks-warm.jpg`** (the warm-sunlit-trunks one, previously in the hero slot) in case it's wanted for another section later — Bedrock Babes or a walk card are good candidates given its aspect ratio.

**Hero photo has a saturation filter applied** (`saturate(0.85)` in `app/page.tsx`) so its greens sit with the muted Lichen/Deep Moss palette rather than reading as bright stock photography. This is a CSS filter, fine for this Next.js mockup — **but it will not survive the Squarespace rebuild as CSS.** Squarespace doesn't offer a reliable per-image CSS-filter hook without custom code injection on every page that uses it, which is a maintenance risk (breaks silently if a template update changes image markup, and has to be reapplied on every new hero-style photo). **When this gets rebuilt in Squarespace, bake the desaturation into the exported image file itself** (in photo-editing software, reduce saturation by roughly the same amount, ~85% of original) rather than relying on a CSS filter. The photo also uses a custom focal point (`object-position: 50% 75%`, i.e. centered horizontally, three-quarters of the way down) so the path and sunlit forest floor stay in frame at every breakpoint instead of a plain center crop losing them — set the equivalent focal point manually when exporting for Squarespace, since its image focal-point picker is a single point, not a CSS-style position value.

## How to Customize

- **Colors:** all defined once in `app/globals.css` — change a hex value there and it updates everywhere.
- **Copy:** edit the text directly in `app/page.tsx`.
- **Add a new page:** create a new folder in `app/` with a `page.tsx` inside, e.g. `app/about/page.tsx` for `/about`.
- **Real images live in `public/images/`**, named for what they show (e.g. `hero-forest.jpg`). That's the convention going forward as real photography replaces the remaining placeholders.

## Squarespace Build Notes

This site is a working reference for a future Squarespace 7.1 rebuild. Two things to carry over deliberately:

**Walk dates → Collection, not hand-built rows.** The three cards in "Upcoming dates" (`app/page.tsx`, the `walkCards` array) are structured as a plain list of items, each with the same fields: image, date, location, time, price. That's intentional — in Squarespace, this should become a Collection (an Events collection is the natural fit) with a Summary Block pulling from it, not three manually duplicated blocks. Whoever's editing dates week to week should be adding/editing collection items, not rebuilding a 4-block group each time.

**Card sections (Corporate, email signup) — CSS dependency.** These two sections show a coloured card (Loam / Birch) sitting inside a section of a different colour. Squarespace can't do a single seamless card across multiple blocks natively — each block only gets its own independently-rounded background. The real build should use this fallback-safe approach:
- **Base state (always on, no CSS needed):** every block in the group (eyebrow, heading, body text, button) gets its own matching background colour, corner radius, and padding, natively, via each block's own Design settings. On its own this looks like a stack of individually-rounded blocks — not the seamless card, but presentable and legible on any Squarespace version.
- **Enhancement (Custom CSS, optional):** target the wrapper Squarespace generates around that block group (inspect the live page to get its exact class — Fluid Engine's row/column wrapper classes aren't stable enough to hardcode here sight-unseen) and zero out the touching edges: remove the border-radius on the inner-facing corners of each block, and zero the gap between them, so they visually merge into one card.
- **Why this is safe:** the enhancement CSS only ever *removes* radius and spacing — it never hides content, repositions anything, or depends on the base state being absent. If Squarespace changes its markup and the wrapper selector stops matching, the CSS silently does nothing and the section just reverts to the base state (individually-rounded blocks). It cannot produce overlapping, clipped, or unreadable content.
- **How to check it's working:** open the section in a browser, inspect the card visually — if you see one continuous rounded rectangle, the enhancement is active; if you see 3–4 separate rounded pieces with small gaps, the enhancement CSS isn't matching (harmless, just less polished) and needs re-pointing at the current markup.

## Recent Changes

- 2026-09-20: Built the homepage from your detailed brief — brand colors/fonts, navigation, footer, and all 9 homepage sections with your exact copy. Flagged placeholder content (guide bio, walk card details, Instagram link) and listed required photo slots above.
- 2026-09-20: Consistency pass — separated the email signup section from Corporate/Footer with a Birch band so colored sections never sit adjacent; converted ambiguous button-styled links to real underlined text links; unified all body text onto two shared width measures; fixed the Meet-the-guide section's image-driven padding asymmetry; matched the footer tagline's letter spacing to the eyebrows; rebuilt Corporate as a contained card instead of a full-bleed band; introduced a two-tier heading hierarchy (What a walk is / Upcoming dates read larger); changed heading font to Fraunces weight 500 with conventional (WONK 0) letterforms; renamed the ambiguous "What a walk is" link to "What to expect."
- 2026-09-20: Made the hero's "What to expect" a real secondary button (was a mismatched text link) matching the primary button's height exactly; reverted email signup back to Lichen so Bedrock Babes/Corporate/Footer don't read as an unbroken Birch run; rebuilt the signup section as a static facts row above a Birch card containing a wider (560px), height-matched email field + button and a reassurance line.
- 2026-09-20: Removed unconfirmed group-size numbers from copy (facts row and "We gather" step now say "small group(s)" instead of a specific number). Removed the Fraunces WONK axis setting — Squarespace can't control it and we couldn't verify it would survive the move, so it wasn't worth the dependency; weight stays at 500. Added Squarespace Build Notes documenting the walk-dates-as-Collection plan and the exact fallback-safe CSS approach for the Corporate/signup card treatments.
- 2026-09-20: Generated a hero forest photo with ChatGPT (real/grounded per Terry's direction, not ethereal) and wired it into the hero section at `public/images/hero-forest.jpg` — the one image on the page that's no longer a plain placeholder. Flagged it clearly as AI-generated and temporary; every other image slot stays a placeholder until real photography arrives.
- 2026-09-20: Rebuilt the hero as a true full-bleed split — the photo now runs edge to edge with no margin, frame, or corner radius, filling the entire right half of the section from the bottom of the nav to the bottom of the section (mobile: full width above the text instead). Replaced the AI-generated placeholder with the client's real supplied photo (resized from 4000×6000 to 2000×3000 for repo size). The hero photo is done — no longer a placeholder and no longer flagged for replacement.
- 2026-09-20: Matched the hero to the approved Figma design — panel color flipped from Lichen to Deep Moss, all hero text to Birch, both buttons to their inverted (dark-background) styles. Added a `secondary-inverted` Button variant for this. Swapped in a second real hero photo (client-supplied, landscape, resized from 6000×3985 to 2400×1594) and applied `saturate(0.65)` so its greens read as part of the palette rather than louder than it — documented as a CSS-only treatment that must be baked into the file before the Squarespace rebuild.
- 2026-09-20: Inspected the actual Figma export directly (not just verbal description) to correct the hero: split changed from 50/50 to the Figma's exact 45% panel / 55% photo; hero now fills the viewport below the header (720px minimum) instead of sitting at roughly a third of it, matching Figma's hero filling exactly to the bottom of its reference frame; added vertical centering of the text block, which the Figma file shows with exactly equal space above and below (confirmed via its layer inspector, not eyeballed). Verified the hero photo was already correct on both local and production (byte-identical, confirmed by hash) — the "dark light-rays" version the client saw was very likely a stale browser cache. Flagged two remaining discrepancies from Figma for the client to confirm rather than silently overriding: Figma's eyebrow text is Lichen, not Birch as explicitly instructed; Figma's heading spec is still Fraunces SemiBold (600), predating the later explicit decision to move all headings to weight 500.
- 2026-09-20: Fixed a real overflow bug the 720px-minimum hero caused on mobile (1041px tall on an 812px screen — the heading needed scrolling to see at all). Hero is now capped at 88vh on mobile with the photo at ~33% of that height and the text block tightened to fit the rest without clipping (a flexbox min-content quirk was silently cutting off the second button before the fix). Desktop target reduced from a 720px floor to a 78vh target clamped between 640–780px, per corrected instructions. Reset the photo's crop from dead-center to `50% 78%` so the sunlit path stays in frame instead of being cropped out, and eased the saturation filter back up from 0.65 to 0.85. Separately, resolved the earlier-flagged eyebrow-color discrepancy: eyebrows on dark backgrounds (hero, Why it works, Corporate, and now the footer tagline) are Lichen, not Birch, so they no longer read as the same color as the heading beside them; all eyebrows sitewide also dropped from weight 600 to 500 to stop reading as shouting.
- 2026-09-20: Reverted the hero photo back to the light-rays-through-the-trees image (was briefly the warm-trunks one) — client's call: it shows a clear path and its own dark/light contrast sits as one scene with the Deep Moss panel rather than competing with it. The warm-trunks photo is kept at `public/images/forest-trunks-warm.jpg` for possible use elsewhere, not deleted. Retuned the focal point for this different composition: `50% 75%` keeps the path and lit forest floor in frame at 375/768/1440. Confirmed the mobile heading and both buttons are still fully visible without scrolling. Cleared the local image-optimizer cache and restarted the dev server (not just a browser refresh) to rule out the stale-cache issue that's recurred a few times this session.
- 2026-09-20: Found and fixed the actual cause of "invisible" placeholders on Upcoming dates: the photo sits flush (no padding) at the top of its Birch card, so the card's own birch background was fully hidden behind it there — meaning a Lichen box in that spot was really sitting against the Lichen section behind the whole card, not the birch card itself. Rebuilt PhotoSlot to take a `background` prop describing what's actually touching its edges and pick the opposite fill automatically, added a 1px Loam/40 border, a small line-art SVG icon, and rounded-lg corners matching the site's cards. Every placeholder on the page now reads as a clearly defined block. Restructured "What a walk is" into three image+numeral+heading+body columns (was a plain stacked list) — the numerals are large (64px) Lichen-on-Birch Fraunces figures, deliberately low-contrast so they read as a quiet visual device rather than competing with the real headings. Restructured "Why it works" the same way: three short findings in columns across the full width instead of one dense paragraph crammed into the left half with ~456px of empty space beside it (now ~0).
- 2026-09-20: Three mobile-only hero fixes: eyebrow drops to 13px/1.4px tracking below 768px (16px read as too heavy once the heading itself shrinks); both hero buttons now hug their labels and sit side by side (12px gap, `!important`-forced padding override so the shared Button component's desktop padding doesn't win by build-order accident) instead of stacking full-width; mobile photo height increased to ~320px with its focal point changed to `50% 50%` so the light rays survive the crop (75% down cut them off entirely at the shorter mobile height). All three combined still fit comfortably under the 88vh cap with no clipping. Converted "More about the practice" from a filled button to a TextLink (Lichen, underlined) to match every other read-more link on the page — it was competing with the hero's primary CTA for attention, which a secondary "read more" shouldn't do. Audited every other button/link on the page against the same rule; the only other exception is the hero's "What to expect," which is a button by explicit, earlier design decision (matches Figma, pairs with the primary CTA beside it), not an oversight. Swapped the Corporate "Enquire" button's hover direction: now Lichen fill by default, brightening to Birch on hover (added a new `primary-lichen` Button variant for this specifically, so it doesn't affect the hero's Birch/Lichen-hover button which stays as the intentional brightest exception on the page).
- 2026-09-20: Renamed the "What a walk is" section's eyebrow to "WHAT TO EXPECT," matching the hero button that links there ("What to expect") and removing the same "where the walk is" ambiguity already fixed on that button. Section heading unchanged. Audited the rest of the site for the same name mismatch — see that day's conversation for the full list; the primary-nav "Walks" item was flagged as a possible but unconfirmed case, since it points to a not-yet-built `/walks` page whose scope isn't necessarily identical to this homepage section.
- 2026-09-20: Fixed the cramped, rhythm-less mobile hero text block — every gap had been 8–16px with no variation, reading as one solid mass. Heading down to 30px on mobile (was 36px) and replaced the ad-hoc margins with a deliberate scale (32/12/16/28/40px, detailed above). The heading still wraps to 4 lines at exactly 375px width (3 lines at 390px+) — narrower than hoped, but the fixed gaps still hold exactly regardless, and the hero comfortably clears the 88vh cap at both widths (86.4% at 375, 80.1% at 390) after trimming the photo from 39% to 37% of the hero's height to reclaim the few pixels the still-4-line heading needed.
- 2026-09-20: Matched the email signup input and button exactly — both were already 48px tall and full-width on mobile (that part wasn't actually broken), but the button's sitewide leaf-shaped corners next to the input's plain rounded corners read as mismatched. Button now uses the same rounded-lg (8px) as the input for this one form-control pairing; every other button on the page keeps the leaf shape.
- 2026-09-20: Collapsed the page's three different filled-button colors (Birch, Lichen, Fern) down to one rule with no exceptions: Fern on light backgrounds, Lichen on dark, both hovering brighter toward Birch on dark / darker toward Deep Moss on light. The hero's "Upcoming dates" button moves from Birch to Lichen — it was the one deliberate exception under the old rule ("brightest element on the page"), now retired in favor of consistency. Renamed the `primary-lichen` variant back to `primary-inverted` since it's now the only dark-background fill option, reclaiming the clearer role-based name. Confirmed Forest Ink on Lichen is 11.30:1 and Lichen separates from Deep Moss at 6.46:1, both comfortably clear of the 4.5:1 requirement.
- 2026-09-20: Mobile hero heading down to 26px (from 30px) — 30px had still wrapped to 4 lines, confirmed by measurement rather than assumed; 26px reliably wraps to 3 at both 375px and 390px. Mobile photo increased from ~37% to ~43% of the hero's height (~349–363px) to offset the heading's freed-up space, keeping the hero's total height roughly flat and still comfortably under the 88vh cap (86.0% at 375, 84.4% at 390). Confirmed the light rays stay visible at the taller crop with the same 50% 50% focal point.
- 2026-09-20: Fixed a live desktop hero bug: the photo becomes absolutely positioned at 768px and up, which silently removed it from the flex row and left the text block as the sole flex child — `flex-1` then stretched it to the full section width instead of the 45% panel, so the heading (on top in stacking order) ran across the photograph and was unreadable where it crossed the light areas. Restored an explicit `md:w-[45%]` on the text block, matching the original 45/55 split, and moved the heading's size jump from the `sm:` breakpoint to `md:` so it changes at the same point as the layout itself. Verified by eye at 768, 1024, 1440, and 1512 with no overlap at any of them; confirmed mobile (375) is unchanged (still 26px, 3 lines, 43vh photo). Added the "Breakpoint scoping" standing rule above as a direct result of this incident.
- 2026-09-20: Two accessibility fixes. (1) The "01/02/03" numerals in "What to expect" were Lichen-on-Birch at 1.35:1 — effectively invisible. Changed to Loam at 40% opacity; the rendered color was confirmed pixel-for-pixel against a hand-calculated sRGB blend, giving a contrast ratio of 1.96:1 against Birch — a real improvement while staying deliberately quiet, since these are decorative and hidden from screen readers (`aria-hidden`). (2) 13 mobile tap targets measured 24px tall (every footer link, every TextLink "read more," the navbar wordmark at 28px) — below the 44px accessible minimum. Fixed by adding vertical padding sized to each element's existing line-height (`py-2.5` for the 24px-line-height links, `py-2` for the 28px wordmark) rather than changing font size, so the visible design is unchanged. All 13 now measure exactly 44px tall at 375px width.
